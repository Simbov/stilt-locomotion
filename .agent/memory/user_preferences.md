---
name: User preferences
description: Working style and preferences observed in this project
type: user
originSessionId: a6d0896f-1841-49f6-a2b9-837c74cd984b
---
- Wants proper, maintainable solutions — no temporary fixes (e.g. copying files into site-packages is unacceptable).
- Expects code to be tested before being reported as done ("make sure to test before finishing").
- Prefers configurable constants over magic numbers (e.g. `MASS_PLAY_MAX_KG` as a named export rather than hardcoding `5.0` in a slider).
- Wants minimal environments in the viewer — 1 env for visualisation, not 4.
- Cares about documentation being up to date and complete before archiving a session.
